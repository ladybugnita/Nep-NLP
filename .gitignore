# ---- Python (ml-service) ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ipynb_checkpoints/

ml-service/models_store/
*.pt
*.bin
*.safetensors
*.onnx

# ---- Java (backend) ----
backend/target/
*.class
.mvn/wrapper/maven-wrapper.jar

# ---- Node / React (frontend) ----
node_modules/
frontend/dist/
frontend/.vite/

# ---- Data ----
# Raw scraped data is not committed (licensing + size). Only curated samples are.
data/raw/
ml-service/data/raw/
*.jsonl.gz
# Derived training file that embeds scraped article text (re-generate from the scraper).
ml-service/data/news_train_full.csv
# Gold-set pool + generated labelling tool embed scraped text (re-generate as needed).
ml-service/data/gold/
ml-service/tools/label_news.html

# ---- Env / secrets ----
.env
*.local

# ---- OS / IDE ----
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

docs/